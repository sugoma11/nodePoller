{
  "name": "figjam-http-plugin",
  "version": "1.0.0",
  "description": "FigJam plugin with HTTP interface for automated content management",
  "scripts": {
      "build": "esbuild code.js --bundle --outfile=dist/code.js --target=es6 && copy ui.html dist\\ && copy ui.js dist\\ && copy manifest.json dist\\",
      "watch": "esbuild code.js --bundle --outfile=dist/code.js --target=es6 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
